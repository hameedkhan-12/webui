{
  "name": "@aura/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"gateway dev server — Subsystem 8\"",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint: not configured yet\""
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "@aura/kernel": "workspace:*",
    "@aura/component-registry": "workspace:*",
    "@aura/schema-engine": "workspace:*",
    "@aura/ast-engine": "workspace:*",
    "@aura/persistence": "workspace:*",
    "ws": "^8.14.0",
    "p-queue": "^7.4.1"
  },
  "devDependencies": {
    "@types/ws": "^8.5.6",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
